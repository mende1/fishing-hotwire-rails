<%= render_update_stats_stream @fish_catches %>

<%= turbo_stream.prepend "catches", @fish_catch %>

<%= turbo_stream.update "new_catch",
                        partial: "form",
                        locals: { fish_catch: @new_catch } %>

<%= render_flash_stream %>